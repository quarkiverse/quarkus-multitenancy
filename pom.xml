<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>io.quarkiverse</groupId>
        <artifactId>quarkiverse-parent</artifactId>
        <version>22</version>
    </parent>

    <groupId>io.quarkiverse.multitenancy</groupId>
    <artifactId>quarkus-multitenancy-parent</artifactId>
    <version>999-SNAPSHOT</version>
    <packaging>pom</packaging>

    <name>Quarkus - Multitenancy - Parent</name>
    <description>
        Generic and configurable multi-tenant resolution support for Quarkus.

        Provides a runtime abstraction for resolving tenant identifiers and making them
        available through a CDI-managed TenantContext. The abstraction can be integrated
        by different transports and extensions such as HTTP or ORM or Security, while keeping
        the core tenant resolution mechanism independent of any specific technology.
    </description>
    <url>https://github.com/quarkiverse/quarkus-multitenancy</url>

    <scm>
        <connection>scm:git:git@github.com:quarkiverse/quarkus-multitenancy.git</connection>
        <developerConnection>scm:git:git@github.com:quarkiverse/quarkus-multitenancy.git</developerConnection>
        <url>https://github.com/quarkiverse/quarkus-multitenancy</url>
        <tag>HEAD</tag>
    </scm>

    <developers>
        <developer>
            <id>mathias82</id>
            <name>Matthaios Stavrou</name>
            <url>https://github.com/mathias82</url>
        </developer>
        <developer>
            <id>lu1tr0n</id>
            <name>Luis Navarro</name>
            <url>https://github.com/lu1tr0n</url>
        </developer>
    </developers>

    <modules>
        <module>quarkus-multitenancy-core-runtime</module>
        <module>quarkus-multitenancy-core-deployment</module>
        <module>quarkus-multitenancy-http-runtime</module>
        <module>quarkus-multitenancy-http-deployment</module>
        <module>quarkus-multitenancy-orm-runtime</module>
        <module>quarkus-multitenancy-orm-deployment</module>
        <module>quarkus-multitenancy-demo</module>
        <module>docs</module>
    </modules>

    <properties>
        <maven.compiler.release>21</maven.compiler.release>
        <quarkus.version>3.37.3</quarkus.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>io.quarkus</groupId>
                <artifactId>quarkus-bom</artifactId>
                <version>${quarkus.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-junit5</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-junit5-internal</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>io.rest-assured</groupId>
            <artifactId>rest-assured</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <pluginManagement>
            <plugins>
                <!--
                  Available for the future docs module: assembles the per-module
                  config-doc model produced by quarkus-extension-processor into
                  Asciidoc output (mandatory pipeline post-Quarkus 3.14).
                -->
                <plugin>
                    <groupId>io.quarkus</groupId>
                    <artifactId>quarkus-config-doc-maven-plugin</artifactId>
                    <version>${quarkus.version}</version>
                </plugin>
                <!--
                  Keep the published Javadoc clean. The only doclint "missing"
                  warnings the build emits are the JDK's nag about the implicit
                  default constructor on CDI-managed beans (resolvers, filters,
                  build processors) that are never instantiated by hand. Adding
                  empty constructors purely to carry a comment would be noise, so
                  the "missing" group is dropped while the higher-signal checks
                  (html, syntax, reference, accessibility) stay enforced.
                -->
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-javadoc-plugin</artifactId>
                    <configuration>
                        <doclint>all,-missing</doclint>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
        <plugins>
            <!--
              Apache-2.0 source headers. Configured (not bound to a phase) so the headers
              can be applied/verified on demand without changing the build for contributors:
                mvn license:format   (apply or update headers)
                mvn license:check    (verify; used in CI if enforcement is desired)
            -->
            <plugin>
                <groupId>com.mycila</groupId>
                <artifactId>license-maven-plugin</artifactId>
                <version>5.0.0</version>
                <configuration>
                    <licenseSets>
                        <licenseSet>
                            <header>${maven.multiModuleProjectDirectory}/header.txt</header>
                            <includes>
                                <include>**/*.java</include>
                            </includes>
                        </licenseSet>
                    </licenseSets>
                    <mapping>
                        <java>SLASHSTAR_STYLE</java>
                    </mapping>
                </configuration>
            </plugin>
        </plugins>
    </build>

</project>
